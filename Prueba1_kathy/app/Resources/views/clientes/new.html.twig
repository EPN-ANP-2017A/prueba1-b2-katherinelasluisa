{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
<div class="main container">
    <div class="row well">
        <div class="cold-md-6">
            <div class="page-header">
    <h1>Cliente creation</h1>
                </div>

            {{ form_start(form, {'attr' : {'role' : 'form'}}) }}

            <div class="form-group">
                {{ form_label(form.nombre) }}
                {{ form_widget(form.nombre, { 'attr' : {'class' : 'form-control', 'placeholder' : 'Your nombre'} }) }}
                <span class="text-danger">{{ form_errors(form.nombre) }}</span>

            </div>
            <div class="form-group">
                {{ form_label(form.cedula) }}
                {{ form_widget(form.cedula, { 'attr' : {'class' : 'form-control', 'placeholder' : 'Your cedula'} }) }}
                <span class="text-danger">{{ form_errors(form.cedula) }}</span>

            </div>


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('clientes_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
